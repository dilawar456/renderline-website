require('dotenv').config({path: '.env.local'}); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function fix() { try { const { error } = await supabase.from('site_content').update({ portfolio_order: JSON.stringify({ 'all': [] }) }).eq('id', 1); console.log('Update error (might fail if missing):', error); } catch(e) { console.error(e); } } fix();
