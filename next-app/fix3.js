require('dotenv').config({path: '.env.local'}); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); supabase.from('site_content').update({ portfolio_pinned: JSON.stringify({'all':[166,160]}) }).eq('id', 1).then(r=>console.log(r));
